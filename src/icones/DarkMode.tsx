import styled from "styled-components";
import { DarkMode } from "styled-icons/material-twotone";
import { Colors } from "../utiles";

export const DarkTheme = styled(DarkMode)`
  color: ${Colors.GRAY_DARK_02};
`;
